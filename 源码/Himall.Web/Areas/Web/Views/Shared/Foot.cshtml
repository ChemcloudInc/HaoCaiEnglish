<div class="footer">
<div class="warp">
	<ul class="site-promise cl">
		<li class="fore1">
			<b></b>
			<h3>Real Guarantee</h3>
			<p>All products are original quality</p>
		</li>
		<li class="fore2">
			<b></b>
			<h3>7*24 Service</h3>
			<p>service anytime</p>
		</li>
		<li class="fore3">
			<b></b>
			<h3>Return & Exchange</h3>
			<p>More Infomation in exchange policy</p>
		</li>
		@*<li class="fore4">
			<b></b>
			<h3>Free Shipping</h3>
			<p>Except for big items</p>
		</li>*@
		<li class="fore5">
			<b></b>
			<h3>Help Center</h3>
			<p>New User Guide</p>
		</li>
	</ul>
    <div class="footer-service cl">
        @{
            int i = 1;
            var pageFootService = (IEnumerable<Himall.Web.Areas.Web.Models.PageFootServiceModel>)ViewBag.PageFootService;
        }
        @if (pageFootService != null)
        {
            foreach (var t in pageFootService)
            {
                if (t != null)
                {
                    <dl class="fore@(i++)">
                        <dt><strong>@t.CateogryName</strong></dt>
                        <dd>
                            @{var articles = (IEnumerable<Himall.Model.ArticleInfo>)t.Articles;}
                            @if (articles != null)
                            {
                                foreach (var article in t.Articles)
                                {
                                    if (null != article)
                                    {
                                        <p><a href="@Url.Action("Details", "Article", new { id = article.Id }) " target="_blank" rel="nofollow">@article.Title</a></p>
                                    }
                                }
                            }
                        </dd>
                    </dl>
                }
            }
        }
		<div class="qrcode">
			<img  class="lazyload" src="@ViewBag.QRCode"/>
			<h3>Like with sweep QR Code </h3> 
			<p>@(ViewBag.SiteName)Official WeiXin</p>
		</div>
    </div>
</div>

<div class="copyright">
	<div class="warp">
		@{string pageFoot = ViewBag.PageFoot==null?string.Empty: ViewBag.PageFoot as string;}
		@Html.Raw(pageFoot)
	</div>
</div>
</div>
@{
    string FootScript = ViewBag.FlowScript;
}

<div style="display:none;">
@Html.Raw(FootScript)
</div>

<script type="text/javascript">
    $(function () {
        //$.ajax({
        //    type: 'get',
        //    url: '/Home/GetFoot',
            
        //    dataType: 'json',
        //    cache: true,// 开启ajax缓存
        //    success: function (data) {
        //        if (data) {
        //            //console.log(data);
        //            var html = '';
        //            for (var i = 0; i < data.length; i++) {
        //                var text = '<dl class="fore' + (i+1).toString() + '"><dt><b></b><strong>' + data[i].CateogryName + '</strong></dt><dd>';
        //                if (data[i].Articles.length > 0) {
        //                    for (var j = 0; j < data[i].Articles.length; j++) {
        //                        text += '<div><a href="/Article/Details/' + data[i].Articles[j].Id.toString() + '" target="_blank" rel="nofollow">' + data[i].Articles[j].Title + '</a></div>';
        //                    }
        //                }
        //                text += '</dd></dl>';
        //                html += text;
        //            }                    
        //            $("#service-2013").prepend($(html));
        //        }
        //    },
        //    error: function (e) {
        //        //alert(e);
        //    }
        //});
    });
</script>